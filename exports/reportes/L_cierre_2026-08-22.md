# [FRENTE L] Cierre — Re-matching masivo del corpus histórico (2026-08-22)

**⚠ SYNC DIFERIDO por presupuesto I/O de Supabase agotado (aviso formal de Supabase). El gate P3 está APROBADO y la medición vale; solo se difiere la publicación: corre consolidado con el backlog NLP la semana próxima** vía `scripts/ops/sync_consolidado_v2.py` (pre-reporte de volumen = punto de control de Gerardo antes de ejecutar).

## Qué se hizo

Re-decisión del matching (SOLO matching, `--skip-nlp`) de todo el corpus histórico anterior al matcher v3.6.0, UNA vez, con las auditorías K+K2+K3 y el traductor en producción. Universo censado **77.040**; al cierre **83.786 ofertas con matching 3.6.x** (incluye nacidas del backlog NLP en la ventana). Corrida 2026-08-20 20:09 → 2026-08-22 03:02 en tandas de 2.500 bajo tmpfs (sha256 + sync-back verificado), TEST del evaluador cada 4 chunks: **todos 35/35**.

## Protecciones (se cumplieron)

- **Candado F0.4b**: 6.275 `validado` + 38 `en_revision` + 13 `rule_manual_fix` excluidas del universo; verificado intacto post-corrida. Ningún DELETE; solo UPDATE de matching (capa skills arrastrada y snapshotteada).
- **Snapshot reversible**: `exports/cohorts/snapshot_pre_rematching_2026-08-19_*` (matching + skills, sha256 en el manifiesto).
- **Remanente 13**: estados de revisión humana intermedia (`validado_claude`/`pendiente_humano_C1`) que el pipeline protege por diseño — documentadas, sin acción.

## Resultado (medición completa: `L_p3_medicion_2026-08-22.md`)

- **44,4% del corpus comparable cambió de destino** (34.236/77.027; dry-run proyectaba 45,0%; 0 pérdidas de cobertura).
- Desglose por causa (población completa): **27,8% corrección-K/K2/K3 · 26,2% retiro-desforzada · 22,8% semántico-mejorado · 12,8% reordenamiento dict/regla · 5,2% traductor · 5,2% resto**. Más de la mitad del cambio = la auditoría de Cyn a escala.
- KPI canales (re-matcheadas): semántico 36,7 / plana 31,5 / dict 14,0 / L3 8,5 / subordinada 5,3 / **árbol 4,0%** (3.345 decisiones; hubs 16 y 51 dominan).
- JOIN cohorts (K 4.686 + K2 548 + K3 160): **cero huérfanas**; K3 retiene el destino retirado solo 1%.
- Caso testigo reparado: «Intendente de obra» 1118027524 → dict → **capataz de construcción** ✓.
- Spot-check 30 clasificado (en `L_p3_resultados_2026-08-22.json`); 1 caso malo honesto («asistente legal bilingüe» des-forzada → semántico absurdo) → insumo mesa.

## Sync consolidado v2 (preparado, NO ejecutado)

`scripts/ops/sync_consolidado_v2.py`:
- **Pre-reporte** (default, no toca Supabase): diff contra snapshot → cuántas filas viajan (cambiadas ∪ nuevas-backlog; skills solo donde el set cambió) + requests y duración estimados. **Es el punto de control de Gerardo.**
- **Ejecutar**: requiere `--ejecutar --aprobado-por-gerardo`; aborta en horario pico (09-21 ART) salvo `--fuera-de-valle`; tandas con pausas (~8 req/s sostenido, mitad del límite del free tier); reusa extracción/transform/upsert del sync canónico (upsert dirigido, NUNCA `--full`).
- Guarda de consistencia: aborta si la BD es symlink (sesión tmpfs ajena) — ya validada en vivo.

## Incidencias operativas de la corrida (todas con fix commiteado)

1. rc=1 del pipeline (= errores esperados en `validation_errors`) tratado como crash → tolerado; solo rc>1 corta.
2. Loop estancado al agotar el universo (las 13 protegidas re-seleccionadas ~1.950 veces, sin escrituras) → guarda anti-estancamiento.
3. Driver original moría en la ventana de sync-back ajena (symlink) → espera symlink + reintento en aborts de contención.
4. Multi-perfil degradado a SINGLE a propósito (sin OLLAMA_HOST): el frente re-decide solo matching.

## Pendientes al cierre

- **P4 diferido**: pre-reporte del sync consolidado post-backlog → OK de Gerardo → ejecutar en valle → spot 10 en dashboard.
- Backlog NLP: corre por su lado (otra sesión), remanente ~5.960 al 2026-08-20.
- Caso «asistente legal bilingüe» y pares (des-forzadas con semántico malo) → próxima ronda de la mesa.
