# [FRENTE I — P2] G2: verificación de entrada del JSON 2.0 — 2026-08-05

Archivo: `docs/MOL_reglas_ESCO_88_ocupaciones_COMPLETO.json` (schema
`modelo_ocupaciones_2.0`). El resumen se autodeclara limpio; verificación independiente:

## 1. Códigos y URIs contra NUESTRO catálogo

- **Códigos principales: 88/88 OK-exacto.** Códigos-destino de las 900 D: **465
  distintos, 465/465 en catálogo.** Cero no-existe, cero otro-concepto.
- **Skills canon: 5.408 URIs recolectadas** (coincide exacto con el resumen). Muestreo
  robusto de 500 (seed fija): **0 no-existen, 0 label-difiere** → se extrapola limpio
  con la salvedad del muestreo. (Coherente: la fuente declarada es nuestro propio
  export `ocupaciones_esco_con_skills.xlsx`.)

**El autodeclarado del resumen se CONFIRMA independientemente.** La guarda no se delegó:
se verificó.

## 2. Diff contra el índice del G (Excel FINAL-8)

- **Prosa: cero divergencia de contenido en las 88** — el JSON estructuró la prosa del
  Excel sin reescribirla.
- **`titulos_aviso`: 87/88 "difieren" del índice G, pero es artefacto NUESTRO** — el
  extractor del frente G no partía los «X o Y» compuestos («asistente contable o
  administrativo/a contable» quedaba como un trigger); el JSON los separa correctamente.
  La estructuración del JSON es MEJOR que nuestro parse; contenido idéntico. (Bug del
  extractor G anotado para no re-usarlo.)
- **Las 3 «duplicadas equivalentes eliminadas» y la 1 «referencia no operativa
  separada»: NO son identificables desde el JSON** — solo existen como números del
  resumen, sin rastro de cuáles fueron (904→900 sin audit trail). **Pregunta puntual
  para Cyn/Juan Domingo.**
- **Anomalía detectada de paso: la regla 11 (coordinador de transporte, 4323.9) tiene
  0 reglas de desambiguación en el JSON**, cuando su prosa del Excel deriva ~10.
  Pregunta puntual (¿se perdieron en la conversión?).

## 3. Censo de compilación — EL NÚMERO DEL P2 DEL SPEC H

De las 988 reglas (88 inclusiones + 900 D):

| Modo | n | % |
|---|---|---|
| **Semántico (prosa-sola, sin `terminos`)** | **975** | **98,7%** |
| Determinista (`terminos` + modo alguna/min_matches/principalmente/solo_estas) | 13 | 1,3% |

Las 13 deterministas están concentradas en la ocupación 1 (analista contable — el molde
1.1 compilado a mano en el harness). **El trabajo de proponer términos a escala es
~975 reglas** — ese es el tamaño exacto del P2 del spec H. 87 de las 88 ocupaciones
llegan con TODAS sus reglas en modo semántico.

## 4. `casos_validacion`

**Confirmado: 0 casos en las 88** — pero la estructura EXISTE en todas
(`casos_incorporados: 0, items: [], estado: pendiente_de_casos_reales`, con archivo
separado sugerido por ocupación). El esquema anticipa la validación por casos; el
contenido está pendiente. Nada muestreable.

## 5. Tecnología-como-definitoria (insumo de la guarda a)

El canal existe en la entrada (`contenidos = [tareas_explicitas, skills_habilidades,
conocimientos, tecnologias, sistemas_herramientas]`) pero **NINGUNA regla declara
tecnología-como-definitoria**: la palabra «tecnología» aparece UNA sola vez en todo el
JSON (el nombre del canal). Lo que sí existe por regla es `tareas_definitorias` (las
88). **Inventario para la guarda (a): 0 reglas con marca explícita** — el spec debe
derivar la definitoriedad tecnológica (p.ej. términos de `sistemas_herramientas` en
condiciones) o preguntarla; el esquema hoy no la porta.
