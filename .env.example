# =====================================================================
# VARIABLES DE ENTORNO - Bumeran Scraping
# =====================================================================
#
# Copiar este archivo a .env y configurar valores reales
# IMPORTANTE: NO subir .env a Git (ya está en .gitignore)
#
# Uso:
#   1. Copiar: cp .env.example .env
#   2. Editar .env con valores reales
#   3. Las variables se cargan automáticamente
# =====================================================================

# =====================================================================
# SQLITE (Base de datos)
# =====================================================================

# Ruta al archivo de base de datos SQLite
# (se crea automáticamente si no existe)
DB_PATH=database/bumeran_scraping.db

# =====================================================================
# ALERTAS (Opcional - Futuro)
# =====================================================================

# Email para alertas
ALERT_EMAIL=tu_email@example.com

# Slack webhook (futuro)
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# =====================================================================
# SCRAPING (Opcional - Sobrescribe defaults)
# =====================================================================

# Delay inicial entre requests (segundos)
# SCRAPING_INITIAL_DELAY=2.0

# Modo incremental por defecto
# SCRAPING_INCREMENTAL=true

# =====================================================================
# SCHEDULER (Opcional)
# =====================================================================

# Días de la semana (comma-separated, 0=lunes, 6=domingo)
# SCHEDULER_DAYS=0,3  # Lunes y Jueves

# Hora de ejecución (formato 24h)
# SCHEDULER_HOUR=8
# SCHEDULER_MINUTE=0

# Timezone
# SCHEDULER_TIMEZONE=America/Argentina/Buenos_Aires
